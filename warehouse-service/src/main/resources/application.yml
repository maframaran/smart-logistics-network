spring:
  application:
    name: warehouse-service
  threads:
    virtual:
      enabled: true   # Java 21 virtual threads (ADR-001)
  datasource:
    url: jdbc:postgresql://localhost:5432/logistics
    username: logistics
    password: logistics
    hikari:
      schema: warehouse
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: warehouse
  flyway:
    schemas: warehouse
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: warehouse-service
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer

kafka:
  topics:
    inventory-received: warehouse.inventory-received
    inventory-dispatched: warehouse.inventory-dispatched
    warehouse-capacity-updated: warehouse.capacity-updated

management:
  endpoints:
    web:
      exposure:
        include: health,readiness,prometheus
  endpoint:
    health:
      probes:
        enabled: true

server:
  port: 8080
